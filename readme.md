# Contract client code generation (Go)

The makefile declares two commands `make gen` and `make clean`.

Whenever the `abi/` folder is updated run `make clean gen` to re-generate the Go clients for the smart contracts.
